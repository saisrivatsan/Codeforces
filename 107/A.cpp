#include <iostream>
#include <stdio.h>
#include <vector>
#include <algorithm>
#include <queue>
#include <map>
#include <string.h>
using namespace std;


int main()
{
    //freopen("A.txt","r",stdin);
    //int n, k, l, c, d, p, nl, np;
    //cin>>n>>k>>l>>c>>d>>p>>nl>>np;

    //cout<<min(min(k*l/nl,c*d),p/np)/n;

    int n,M;
    cin>>n>>M;
    cout<<n<<M;
    return 0;
}
